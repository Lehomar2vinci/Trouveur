name: trouveur
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.1.0-126.0.dev <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  path_provider: ^2.0.2
  provider: ^6.0.5
  path: ^1.8.3
  faker: ^2.1.0
  convert: ^3.1.1
  file: ^6.1.4
  matcher: ^0.12.15
  go_router: ^2.4.2
dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
   - assets/images/